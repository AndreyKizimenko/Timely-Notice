describe('Core authentication', () => {
  it('Authentication page title', () => {
    cy.visit('http://localhost:3000')
  })
  it('Authentication page components', () => {
    cy.visit('http://localhost:3000')
  })
})